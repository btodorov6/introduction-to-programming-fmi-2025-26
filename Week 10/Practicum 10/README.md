# УП - Практикум 10 - 04.12.2025

### Задачи

---

**Задача 1.** Напишете функция, която приема референция към указател и му променя стойността на nullptr.

---

**Задача 2.** Да се напише функция swap, която приема 2 указателя към числа и разменя стойностите на числата.

---

**Задача 3.** Напишете програма, която извежда елементите на масив без използването на оператор `[]`.

---

**Задача 4.** Напишете функция, която взима два пойнтера - единият към int, а другият към пойнтер към int, и казва дали вторият пойнтер сочи към първия.

---

**Задача 5.** Напишете функция, която приема референция към `float` и връща стойността на цялото число, образувано от прочитането на същата памет, която ползва `float`-а, като `int`.

---

**Задача 6.** Напишете програма, която въвежда масив от N числа и изведете адреса на най-голямото от тях.

----

**Задача 7.**
Направете свои имплементации на
`strcpy`, `strcat`, `strlen`, `strcmp`.

---

**Задача 8.** Да се напише програма, която установява дали даден низ е палиндром, т.е. четен отляво надясно и отдясно наляво е един и същ.

---

**Задача 9.** Да се напише функция, която приема 2 символни низа. Функцията да запазва във втория низ само малките букви на първия.

**Пример:**

Вход:

```
aBc!?Def
```

Изход:
```
acef
```
---

**Задача 10.** Да се напише функция, която приема символен низ и 2 цели числа beg и end и връща substring на въведения от индекс beg до индекс end. Ако end е повече от дължината на стринга, връщаме стринга до края. Ако beg > дължината, връщаме празен низ.

**Пример:**

Вход:

```
Hello 1 3
```

Изход:
```
ell
```

Вход:

```
Hello 1 6
```

Изход:
```
ello
```

Вход:

```
Hello 5 6
```

Изход:
```
```

---

**Задача 11.** Да се напише функция, която приема символен низ и 3 символа `x`, `a` и `b` и замества всяко четно срещане на `x` с `a`, а всяко нечетно - с `b`.`.

**Пример:**

Вход:

```
"H_ll_ my fri_nd" _ e o
```

Изход:
```
"Hello my friend"
```

---

**Задача 12.** Да се напише функция, която приема 3 символни низа и връща най-дългия им общ префикс.

**Пример:**

Вход:

```
diferencialno i integralno smqtane
diskretni strukturi
diskopatiq
```

Изход:

```
di
```

---

**Задача 13.** Да се напише функция, която приема символен низ (съставен само от малки букви) и изтрива всички повтарящи се букви в него.

**Пример:**

Вход:

```
shteseborqzatebechuek
```

Изход:

```
shteborqzacuk
```

---

**Задача 14.** _(цензура)_ Да се напише функция, която приема 2 символни низа - `text` и `substring` и замества всяко срещане на отделна дума с текст равен на `substring` в `text` с толкова звездички, колкото е дължината на `substring` (ако се игнорира разликата между главни и малко букви).

**Пример:**

Вход:

```
Howdy! How are you? How was your day?
how
```

Изход:

```
***dy! *** are you? *** was your day?
```

---

**Задача 15.**\*  Йода от Mеждузвездни войни говори в необичаен словоред. С течение на годините, се установява, че словоредът му има определенa подредба - Йода слага първата дума, след това последната дума, после втората дума, след това предпоследната дума и т.н. докато не се срещнат по средата. Напишете програма на C++, която да превръща едно изречение на Йода в нормален словоред.

**Пример:**

Вход:
```
Koito reshava UP zadachi iska Bansko da v vzeme i
```

Изход:
```
Koito UP iska da vzeme i v Bansko zadachi reshava.
```
